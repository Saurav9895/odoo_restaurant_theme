<odoo>
  <template id="custom_signup_template" name="Styled Signup Page">
    <t t-call="website.layout">
      <!-- <t t-set="title">Signup - Foodie Platform</t> -->

      <style>
        .custom-signup-section {
            margin: 0;
            padding: 0;
            background: url('/theme_yourhome/static/src/img/login_banner.jpg') no-repeat center center;
            background-size: cover;
            font-family: 'Segoe UI', sans-serif;
            display: flex;
            justify-content: flex-end;
            align-items: center;
            min-height: 90vh;
            padding-right: 5%;
        }

        .signup-card {
            background-color: white;
            padding: 40px 35px;
            border-radius: 16px;
            box-shadow: 0 6px 30px rgba(0, 0, 0, 0.15);
            max-width: 420px;
            width: 100%;
            text-align: center;
            margin-top: 20px;
            margin-bottom: 20px;
        }

        .signup-card img.logo {
            width: 70px;
            margin-bottom: 10px;
        }

        .signup-card h2 {
            font-size: 26px;
            margin-bottom: 10px;
            color: #222;
            font-weight: 700;
        }

        .signup-card p {
            font-size: 14px;
            color: #666;
            margin-bottom: 30px;
        }

        .form-group {
            text-align: left;
            margin-bottom: 20px;
        }

        .form-group label {
            display: block;
            margin-bottom: 6px;
            font-size: 14px;
            font-weight: 500;
            color: #444;
        }

        .form-group input {
            width: 100%;
            padding: 12px 15px;
            border-radius: 8px;
            border: 1px solid #ccc;
            font-size: 14px;
            transition: border-color 0.3s;
        }

        .form-group input:focus {
            border-color: var(--primary);
            outline: none;
        }

        .signup-btn {
            width: 100%;
            background-color: var(--primary);
            color: white;
            border: none;
            padding: 14px;
            border-radius: 8px;
            font-size: 16px;
            font-weight: bold;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        .signup-btn:hover {
            background-color: var(--primary);
        }

        .login-link {
            margin-top: 20px;
            font-size: 14px;
        }

        .login-link a {
            color: var(--primary);
            font-weight: bold;
            text-decoration: none;
        }

        .login-link a:hover {
            text-decoration: underline;
        }
      </style>

      <section class="custom-signup-section">
        <div class="signup-card">
          <img src="/theme_yourhome/static/src/img/logo.jpeg" alt="Logo" class="logo"/>
          <h2>Create Your Account</h2>
          <p>Join our foodie family and explore delicious recipes!</p>

          <form method="post" action="/web/signup?redirect=/account" onsubmit="return validatePasswords();">
            <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>

            <div class="form-group">
              <label>Email Address</label>
              <input type="text" name="login" required="required" placeholder="Enter your email"/>
            </div>

            <div class="form-group">
              <label>Full Name</label>
              <input type="text" name="name" required="required" placeholder="Enter your name"/>
            </div>

            <div class="form-group">
              <label>Password</label>
              <input type="password" name="password" id="password" required="required" placeholder="Create a password"/>
            </div>

            <div class="form-group">
              <label>Confirm Password</label>
              <input type="password" name="confirm_password" id="confirm_password" required="required" placeholder="Confirm your password"/>
            </div>

            <button class="signup-btn" type="submit">SIGN UP</button>
          </form>

          <div class="login-link">
            Already have an account? <a href="/web/login">Login</a>
          </div>
        </div>
      </section>

      <script>
        function validatePasswords() {
          const pw = document.getElementById("password").value;
          const cpw = document.getElementById("confirm_password").value;
          if (pw !== cpw) {
            alert("Passwords do not match!");
            return false;
          }
          return true;
        }
      </script>
    </t>
  </template>
</odoo>
