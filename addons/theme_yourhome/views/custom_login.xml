<odoo>
  <template id="custom_login_template" name="Styled Login Page with Header/Footer" inherit_id="web.login">
    <xpath expr="//t[@t-call='web.login_layout']" position="replace">
      <t t-call="website.layout">
        <!-- <t t-set="title">Login - Foodie Platform</t> -->

        <style>
          .custom-login-section {
              margin: 0;
              padding: 0;
              background: url('/theme_yourhome/static/src/img/login_banner.jpg') no-repeat center center;
              background-size: cover;
              font-family: 'Segoe UI', sans-serif;
              display: flex;
              justify-content: flex-end;
              align-items: center;
              min-height: 90vh;
              padding-right: 5%;
                  }

          .login-card {
              background-color: white;
              padding: 40px 35px;
              border-radius: 16px;
              box-shadow: 0 6px 30px rgba(0, 0, 0, 0.15);
              max-width: 420px;
              width: 100%;
              text-align: center;
          }

          .login-card img.logo {
              width: 70px;
              margin-bottom: 10px;
          }

          .login-card h2 {
              font-size: 26px;
              margin-bottom: 10px;
              color: #222;
              font-weight: 700;
          }

          .login-card p {
              font-size: 14px;
              color: #666;
              margin-bottom: 30px;
          }

          .form-group {
              text-align: left;
              margin-bottom: 20px;
          }

          .form-group label {
              display: block;
              margin-bottom: 6px;
              font-size: 14px;
              font-weight: 500;
              color: #444;
          }

          .form-group input {
              width: 100%;
              padding: 12px 15px;
              border-radius: 8px;
              border: 1px solid #ccc;
              font-size: 14px;
              transition: border-color 0.3s;
          }

          .form-group input:focus {
              border-color:var(--primary);
              outline: none;
          }

          .options {
              display: flex;
              justify-content: space-between;
              align-items: center;
              margin-bottom: 25px;
              font-size: 13px;
              color: #555;
          }

          .options a {
              color:var(--primary);
              text-decoration: none;
              font-weight: 500;
          }

          .login-btn {
              width: 100%;
              background-color:var(--primary);
              color: white;
              border: none;
              padding: 14px;
              border-radius: 8px;
              font-size: 16px;
              font-weight: bold;
              cursor: pointer;
              transition: background-color 0.3s;
          }

          .login-btn:hover {
              background-color:var(--primary);
          }

          .signup {
              margin-top: 20px;
              font-size: 14px;
          }

          .signup a {
              color:var(--primary);
              font-weight: bold;
              text-decoration: none;
          }

          .signup a:hover {
              text-decoration: underline;
          }
        </style>

        <section class="custom-login-section">
          <div class="login-card">
            <img src="/theme_yourhome/static/src/img/logo.jpeg" alt="Logo" class="logo"/>
            <h2>Login</h2>
            <p>More than <strong style="color:var(--primary);">15,000 recipes</strong> from around the world!</p>

            <form method="post" action="/web/login?redirect=/account">
              <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>

              <div class="form-group">
                <label>Email Address</label>
                <input type="text" name="login" required="required" placeholder="Enter your email"/>
              </div>

              <div class="form-group">
                <label>Password</label>
                <input type="password" name="password" required="required" placeholder="Enter your password"/>
              </div>

              <div class="options">
                <label><input type="checkbox" name="remember"/> Remember me</label>
                <a href="#">Forgot Password?</a>
              </div>

              <button class="login-btn" type="submit">LOGIN</button>
            </form>

            <div class="signup">
              Don’t have an account? <a href="/custom/signup">Sign Up</a>
            </div>
          </div>
        </section>
      </t>
    </xpath>
  </template>
</odoo>
