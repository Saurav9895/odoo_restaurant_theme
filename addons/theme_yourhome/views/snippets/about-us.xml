<odoo>
  <template id="aboutus_section" name="About Us Section">
    <section class="aboutus-wrapper" id="aboutus">
      <div class="aboutus-content">
        <p class="aboutus-tag">ABOUT US</p>
        <h2 class="aboutus-title">Welcome to our Zetta <br /> Restaurant</h2>
        <p class="aboutus-desc">
          Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.
        </p>
        <button class="aboutus-button">Know More</button>
      </div>
      <div class="aboutus-image">
        <img t-att-src="'/theme_yourhome/static/src/img/food.jpeg'" alt="Delicious Salad" />
      </div>

      <style type="text/css">
        <![CDATA[
        .aboutus-wrapper {
          display: flex;
          align-items: center;
          justify-content: space-between;
          padding: 0 40px;
          font-family: 'Inter', sans-serif;
          background-color: #fff;
        }

        .aboutus-content {
          max-width: 50%;
          margin-left: 80px;
         
        }

        .aboutus-tag {
          color: var(--primary);
          font-size: 14px;
          font-weight: 700;
          letter-spacing: 2px;
          text-transform: uppercase;
          margin-bottom: 10px;
        }

        .aboutus-title {
          font-size: 42px;
          font-weight: 800;
          line-height: 1.2;
          margin-bottom: 30px;
        }

        .aboutus-desc {
          font-size: 18px;
          color: #555;
          line-height: 1.6;
          margin-bottom: 40px;
          max-width: 450px;
        }

        .aboutus-button {
          background-color: var(--primary);
          color: #fff;
          padding: 14px 30px;
          border: none;
          border-radius: 40px;
          font-weight: 600;
          font-size: 16px;
          cursor: pointer;
          transition: background 0.3s ease;
        }

        .aboutus-button:hover {
          background-color: var(--primary);
        }

        .aboutus-image {
          max-width: 50%;
          display: flex;
          justify-content: center;
        }

        .aboutus-image img {
          width: 100%;
          height: auto;
          object-fit: contain;
          margin-right:20px;
        }

        @media (max-width: 900px) {
          .aboutus-wrapper {
            flex-direction: column;
            text-align: center;
            padding: 40px 20px;
          }

          .aboutus-content {
            max-width: 100%;
            padding: 0;
          }

          .aboutus-image {
            margin-top: 30px;
            max-width: 100%;
          }
        }

        @media (max-width: 600px) {
          .aboutus-title {
            font-size: 28px;
          }

          .aboutus-desc {
            font-size: 16px;
          }

          .aboutus-button {
            font-size: 15px;
            padding: 12px 24px;
          }

          .aboutus-image img {
            width: 260px;
          }
          .aboutus-content {
            margin-left: 0;
            
          }
          .aboutus-image img {
          
          margin-right:0;
        }
         
        }
        }
        ]]>
      </style>
    </section>
  </template>
</odoo>
